package ch14;import java.util.LinkedHashMap;import java.util.Map;public class PetCount3 {	static class PetCounter extends			LinkedHashMap<Class<? extends Pet>, Integer> {		public PetCounter() {//			super(MapData.map(LiteralPetCreator.allTypes,0));//			super.put(LiteralPetCreator.allTypes,0);			for(Class<? extends Pet> class1 : LiteralPetCreator.allTypes){				super.put(class1, 0);			}		}				public void count(Pet pet){			for(Map.Entry<Class<? extends Pet> ,Integer> pair:entrySet())			{				if (pair.getKey().isInstance(pet)) {					put(pair.getKey(), pair.getValue()+1);				}			}		}					}	/**	 * @param args	 */	public static void main(String[] args) {		// TODO Auto-generated method stub		PetCounter count = new PetCounter();		for(Pet pet:Pets.createArray(10)){			System.out.print(pet.getClass().getSimpleName()+" ");			count.count(pet);		}		System.out.println(count);	}}